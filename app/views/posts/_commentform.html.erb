<%= form_with(model: [post, comment], url: post_comment_path(post, comment)) do |f|%>

<div class="field"> 
    <%= f.label :content%>
    <%= f.text_area :content%>
</div>

<div class="actions">
<%= f.submit%>
</div>

<% end%>